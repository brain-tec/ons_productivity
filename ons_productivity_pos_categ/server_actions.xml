Sync POS Categories
	
Modèle de base 	Catégorie d'articles
Action à effectuer 	Execute Python Code

categ_ids = env.context.get('active_ids')
model.browse(categ_ids).sync_with_pos_categ()

------------------------------------------------------

Update POS category

Modèle de base 	Modèle d'article
Action à effectuer 	Execute Python Code

product_tmpl_ids = env.context.get('active_ids')
for product in model.browse(product_tmpl_ids):
    pos_categ_id = env['pos.category'].search([('internal_categ_id', '=', product.categ_id.id)])
    if pos_categ_id:
        product.write({'pos_categ_id': pos_categ_id.id})